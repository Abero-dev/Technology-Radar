import PropTypes from 'prop-types';

const RadarGrid = ({ config }) => {
    return (
        <g>
            {/* Aquí iría la lógica para dibujar las líneas de la cuadrícula */}
        </g>
    );
};

RadarGrid.propTypes = {
    config: PropTypes.object.isRequired,
};

export default RadarGrid;